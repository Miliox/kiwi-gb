
pub struct Mmu {

}

impl Mmu {
    // Read a single byte from memory
    pub fn read(&self, addr: u16) -> u8 { 0 }

    // Write a single byte to memory
    pub fn write(&mut self, addr: u16, data: u8) { }
}